{
  "name": "workers3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:key": "wrangler secret put S3_ACCESS_KEY",
    "secret:secret": "wrangler secret put S3_SECRET_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
